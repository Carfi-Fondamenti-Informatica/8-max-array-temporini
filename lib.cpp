#include "lib.h"
float maxarray(float array[],int dim){

    array[dim];
    float max= array[0];
    for(int i=0; i<dim;i++){
        if(max<numeri[i]){
            max= numeri[i];
        }
    }
    return max;
}
